---

---

<div class="pt-8 pb-10 grid mx-auto px-3 text-center">
  <p class="mx-auto">
    <span
      class="text-xl md:text-2xl lg:text-4xl font-bold text-slate-600 dark:text-gray-400"
      >+</span
    >
    <span
      class="num text-2xl md:text-3xl lg:text-5xl text-slate-800 dark:text-gray-100 font-extrabold"
      data-val="35">000</span
    >
    <span
      class="text-xl md:text-2xl lg:text-4xl font-bold text-slate-600 dark:text-gray-400"
    >
      años como docente.</span
    >
  </p>

  <p class="mx-auto">
    <span
      class="text-xl md:text-2xl lg:text-4xl font-bold text-slate-600 dark:text-gray-400"
      >+</span
    >
    <span
      class="num text-2xl md:text-3xl lg:text-5xl text-slate-800 dark:text-gray-100 font-extrabold"
      data-val="240">000</span
    >
    <span
      class="text-xl md:text-2xl lg:text-4xl font-bold text-slate-600 dark:text-gray-400"
    >
      alumnos tomaron clases.</span
    >
  </p>

  <p class="mx-auto">
    <span
      class="text-xl md:text-2xl lg:text-4xl font-bold text-slate-600 dark:text-gray-400"
      >+</span
    >
    <span
      class="num text-2xl md:text-3xl lg:text-5xl text-slate-800 dark:text-gray-100 font-extrabold"
      data-val="1530">000</span
    >
    <span
      class="text-xl md:text-2xl lg:text-4xl font-bold text-slate-600 dark:text-gray-400"
    >
      partituras, audios, videos.</span
    >
  </p>
</div>

<script>
  let valueDisplays = document.querySelectorAll(".num");
  let duration = 1000;
  valueDisplays.forEach((valueDisplay) => {
    let startValue = 0;
    let endValue = parseInt(valueDisplay.getAttribute("data-val"));
    let startTime = null;

    function updateValue(timestamp) {
      if (!startTime) startTime = timestamp;
      let progress = timestamp - startTime;
      startValue = Math.min((progress / duration) * endValue, endValue);
      valueDisplay.textContent = Math.floor(startValue).toString();
      if (progress < duration) {
        requestAnimationFrame(updateValue);
      }
    }

    requestAnimationFrame(updateValue);
  });
</script>
